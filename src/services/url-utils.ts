// Re-export URL utilities for convenience
export {
  isValidUrl,
  normalizeUrl,
  extractDomain,
  ensureProtocol,
  isInternalLink,
  resolveUrl,
  getUrlDepth,
  getFileExtension,
} from '../utils/url-validator.js';
